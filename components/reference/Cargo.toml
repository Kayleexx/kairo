[workspace]
members = ["doc", "doc-redact", "video", "video-frame"]
resolver = "3"

[workspace.package]
edition = "2024"
license = "MIT"
rust-version = "1.95"

[profile.release]
lto = true
codegen-units = 1
strip = true
opt-level = 3
panic = "abort"
