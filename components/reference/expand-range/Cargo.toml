[package]
name = "kairo-expand-range"
version = "0.1.0"
description = "Expands a number into a numeric range upper bound (multiplies by 10,000)"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.61.1"
