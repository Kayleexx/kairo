[package]
name = "kairo-count-primes"
version = "0.1.0"
description = "Counts prime numbers up to a given upper bound"
edition = "2024"
license = "MIT"
rust-version = "1.95"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.61.1"
